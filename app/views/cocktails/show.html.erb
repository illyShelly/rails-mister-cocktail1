<h2><%= @cocktail.name %> </h2>

<h3>Doses</h3>

<ul> <% @cocktail.doses.each do |dose| %>
  <li> <%= dose.description %> </li>
  <%= link_to "Delete", dose_path(dose), method: "DELETE"  %>
  <!-- verb method DELETE-->
  <!-- <li>  dose.ingredient.name </li> -->
  <% end %>
</ul>


<h3>Ingredients</h3>

<ul> <% @cocktail.ingredients.each do |ingredient| %>
  <li> <%= ingredient.name %> </li>
  <% end %>
</ul>

<!-- link to delete whole cocktail -->

<h4>Delete cocktail</h4>
<%= link_to "Delete cocktail", cocktail_path(@cocktail), method: "DELETE"  %>

<br>

<!-- Checking path in rails routes prefix -->
<%= link_to "Back to cocktails", cocktails_path  %>
<%= link_to 'Create new dose',  new_cocktail_dose_path(@cocktail.id) %>

<br>

<%= link_to 'Create new cocktail', new_cocktail_path %>


<!-- we changes routes to dose DELETE /doses/:id(.:format)  doses#destroy -->
  <!-- < link_to "Delete", cocktail_dose_path(@cocktail, dose), method: "DELETE"  > -->
